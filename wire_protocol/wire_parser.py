##interpret commands and 


# class named WireParser that buffers strings until a newline is passed
class WireParser:
    def __init__(self):
        self.buffer = ''
        
    def feed(self, data):
        self.buffer += data.decode();
        if self.buffer
        
        